# NaviHab Deploy Platform

Автоматизированная платформа для деплоя сайтов с помощью AI-агентов.

## Быстрый старт
1. `cd backend && pip install -r requirements.txt && python main.py`
2. `cd frontend && python -m http.server 8080`
